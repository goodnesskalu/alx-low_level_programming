#include <stdio.h>
/**
 * main - prints all single digits of base 10
 * starting from 0
 * Return: 0 if successful
 */
int main(void)
{
	int a;

	for (a = 58; a < 68; a++)
	{
		putchar(a);
	}
	putchar('\n');
	return (0);
}
